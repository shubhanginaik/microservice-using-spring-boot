

spring:
  application:
    name: employee-service
  config:
    import: "optional:configserver:http://localhost:8888"
server:
  port: 8082

zipkin:
    base-url: "http://localhost:9411"
sleuth:
    sampler:
      probability: 1.0